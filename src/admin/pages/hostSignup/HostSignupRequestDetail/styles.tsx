import styled from 'styled-components';

const SHostRequestImageArea = styled.div`
  display: flex;
  gap: 20px;
  min-height: 300px;

  > img {
    width: 50%;
    max-width: 400px;
  }
`;

export { SHostRequestImageArea };
